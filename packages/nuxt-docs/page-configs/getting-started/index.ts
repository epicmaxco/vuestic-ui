import { definePageConfig } from "~~/types/page-config";

export default definePageConfig({
  blocks: [],
  childOrder: [
    'installation',
    'accessibility-guide',
    'configuration-guide'
  ]
});

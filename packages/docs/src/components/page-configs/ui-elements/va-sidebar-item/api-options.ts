import { ManualApiOptions } from 'vuestic-ui/src/services/api-docs/ManualApiOptions'

export default {
  props: {
    activeColor: {
      local: true,
    },
    hoverColor: {
      local: true,
    },
    active: {
      local: true,
    },
  },
  events: {
  },
  methods: {
  },
  slots: {
  },
} as ManualApiOptions

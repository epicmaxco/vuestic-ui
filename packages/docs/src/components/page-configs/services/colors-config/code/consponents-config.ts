export const componentsConfigCode = `
  setGlobalConfig({
    colors: {
      primary: '#ff00ff',
      button: '#000'
    },
    components: {
      VaButton: {
        color: 'button'
      }
    }.
  })
`

export const componentsConfigCode = `
import { mergeGlobalConfig } from 'vuestic-ui'

mergeGlobalConfig({
  colors: {
    primary: '#ff00ff',
    button: '#000'
  },
  components: {
    VaButton: {
      color: 'button'
    }
  }.
})
`

import { mount } from '@vue/test-utils'

import Va{{properCase name}} from '../Va{{properCase name}}.vue'

describe('Va{{properCase name}}', () => {
  it('should render without an error', () => {
    const wrapper = mount(Va{{properCase name}})
    expect(wrapper.findComponent('Va{{properCase name}}')).toBeTruthy()
  })
})

import { ManualApiOptions } from 'vuestic-ui/src/services/api-docs/ManualApiOptions'

export default {
  props: {
    fit: {
      local: true,
    },
    // ItemLabel
    caption: {
      local: true,
    },
    lines: {
      local: true,
    },
    // ItemSection
    icon: {
      local: true,
    },
    avatar: {
      local: true,
    },
  },
  events: {
  },
  methods: {
  },
  slots: {
  },
} as ManualApiOptions

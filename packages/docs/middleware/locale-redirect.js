export default function ({route, redirect, app}) {
    // console.log(app.$root, app.context, )
    // const locales = ['en','es']
    // const isExistingLocale = locales.some(locale => route.fullPath.match(new RegExp(`^\/${locale}(\/|$)`)))
    // if (!isExistingLocale) {
    //     redirect('301', `/${locales[0]+route.fullPath}`);
    // }
}
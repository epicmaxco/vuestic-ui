{
  "title": "Confirm",
  "description": "A simple modal with message and ok and cancel buttons. Returns promise with `true` if ok clicked and `false` if cancel is clicked.",
  "examples": {
    "props": {
      "title": "Props",
      "text": "Any prop from VaModal can be passed to confirm. Event listeners can be passed with `on` prefix. For example, `onClickOutside`."
    }
  }
}